import Head from 'next/head';
import styles from '/styles/components/Login.module.scss';

// MUI
import { blue } from '@mui/material/colors';
import { TextField, Button } from '@mui/material';

// Icons
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faListCheck } from '@fortawesome/free-solid-svg-icons';

// React
import { useEffect } from 'react';

export default function Home() {
  // Set MUI variables
  useEffect(() => {
    document.documentElement.style.setProperty('--login-main-border-color', blue[400]);
    // document.documentElement.style.setProperty('--login-container-width', '80vw');
  }, []);

  return (
    <div className={styles['login__container']} maxWidth='xl'>
      <Head>
        <title>Create Next App</title>
        <meta name='description' content='Generated by create next app' />
        <link rel='icon' href='/favicon.ico' />
      </Head>

      <main className={styles['login__main']}>
        <div className={styles['login__main--icon']}>
          <FontAwesomeIcon icon={faListCheck} />
        </div>

        <form action='' className={styles['form__container']}>
          {/* email */}
          <TextField
            required
            className='login__input--email'
            label='Email'
            variant='outlined'
          />

          {/* password */}
          <TextField
            id='login__input--password'
            className='login__input--password'
            label='Password'
            type='password'
            variant='outlined'
          />

          <div className={styles['form__actions']}>
            {/* login */}
            <Button className={styles['form__actions--button']} variant='contained'>Login</Button>
            {/* sign up */}
            <Button className={styles['form__actions--button']} variant='outlined'>Sign Up</Button>
          </div>
        </form>
      </main>
    </div>
  );
}
